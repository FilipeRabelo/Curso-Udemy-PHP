CRIANDO TABELAS
As tabelas ficam dentro dos bancos, e os dados ficam dentro das tabelas, ou seja, é parte fundamental do banco relacional;
Podemos criar tabelas facilmente por SQL, o comando é: 

CREATE TABLE nome (
	Coluna   tipoDeDado,
	Coluna2  tipoDeDado
);
